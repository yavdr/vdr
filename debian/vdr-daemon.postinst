#! /bin/sh -e

case "$1" in
	abort-upgrade)
		update-alternatives --install /usr/bin/vdr vdr /usr/bin/vdr-daemon 100
	;;
	configure)
		update-alternatives --install /usr/bin/vdr vdr /usr/bin/vdr-daemon 100
	;;
esac

#DEBHELPER#

exit 0
